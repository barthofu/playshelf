$schema: 'https://moonrepo.dev/schemas/project.json'

type: 'application'

fileGroups:
  web:
    - 'src/**/*'
    - 'vite.config.json'
    - 'tsconfig.json'
    - 'tsconfig.node.json'
    - 'svelte.config.js'
    - '/tsconfig.options.json'
  
tasks:

  dev:
    command: 'pnpm tauri dev'
    local: true